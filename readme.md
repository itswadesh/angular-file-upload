# Multiple file upload example using AngularJS and NodeJS

Uploads the files into uploads directory

## Installation

Open Command prompt / Terminal and browse to the directory `multiple-file-uploader`

Run the following command

`npm install`
`node server`
